import { ValidaIdadeMininaDirective } from './valida-idade-minina.directive';

describe('ValidaIdadeMininaDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaIdadeMininaDirective();
    expect(directive).toBeTruthy();
  });
});
